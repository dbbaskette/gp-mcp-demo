spring:
  application:
    name: gp-chat
  config:
    import: optional:file:/app/config/personas.yaml

server:
  port: 8080
  forward-headers-strategy: native

management:
  endpoints:
    web:
      exposure:
        include: health, info

logging:
  level:
    com.baskettecase.gpchat: INFO
