# Copy to gp-chat/.env (git-ignored) and fill in whichever keys you have.
# Missing keys => that provider is silently dropped from the model picker.
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
